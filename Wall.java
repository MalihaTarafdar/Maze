import java.awt.*;

//this class only exists because the directions require it

public class Wall extends Entity {
	public Wall(int x, int y, int width, int height, Color color) {
		super(x, y, width, height, color);
	}
}
